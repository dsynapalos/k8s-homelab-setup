apiVersion: v1
kind: Service
metadata:
  name: matrix-bridge
  namespace: monitoring
  labels:
    app: matrix-bridge
spec:
  selector:
    app: matrix-bridge
  ports:
  - name: alertmanager
    port: 3000
    targetPort: 3000
  - name: harbor
    port: 3001
    targetPort: 3001
