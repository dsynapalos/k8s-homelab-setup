apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: otel-collector-local
  namespace: monitoring
  labels:
    app: otel-collector-local
spec:
  selector:
    matchLabels:
      app: otel-collector-local
  template:
    metadata:
      labels:
        app: otel-collector-local
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8888"
    spec:
      serviceAccountName: otel-collector
      tolerations:
      - operator: Exists
      containers:
      - name: otel-collector
        image: harbor.k8s.local/dockerhub-cache/otel/opentelemetry-collector-contrib:0.145.0
        args:
          - '--config=file:/etc/otelcol/otel-node-base-config.yaml'
          - '--config=file:/etc/otelcol/otel-node-metrics-pipeline.yaml'
          - '--config=file:/etc/otelcol/otel-node-kubeletstats-pipeline.yaml'
          - '--config=file:/etc/otelcol/otel-node-logs-pipeline.yaml'
          - '--config=file:/etc/otelcol/otel-node-traces-pipeline.yaml'
        env:
        - name: K8S_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: K8S_HOST_IP
          valueFrom:
            fieldRef:
              fieldPath: status.hostIP
        ports:
        - containerPort: 4317
          name: otlp-grpc
        - containerPort: 4318
          name: otlp-http
        - containerPort: 8888
          name: metrics
        volumeMounts:
        - name: otel-collector-config
          mountPath: /etc/otelcol/
        - name: varlogpods
          mountPath: /var/log/pods
          readOnly: true
        resources:
          requests:
            cpu: 10m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 2Gi
        readinessProbe:
          httpGet:
            path: /
            port: 13133
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 13133
          initialDelaySeconds: 10
          periodSeconds: 15
      volumes:
      - name: otel-collector-config
        projected:
          sources:
          - configMap:
              name: otel-node-base-config
          - configMap:
              name: otel-node-metrics-pipeline
          - configMap:
              name: otel-node-kubeletstats-pipeline
          - configMap:
              name: otel-node-logs-pipeline
          - configMap:
              name: otel-node-traces-pipeline
      - name: varlogpods
        hostPath:
          path: /var/log/pods
