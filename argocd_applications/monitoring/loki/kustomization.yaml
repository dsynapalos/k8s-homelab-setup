apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - statefulset.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: loki-config
    files:
      - loki-config.yaml
