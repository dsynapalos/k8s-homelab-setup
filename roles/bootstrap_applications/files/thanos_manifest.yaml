apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: thanos
  namespace: argocd
  labels:
    category: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  destination:
    namespace: monitoring
    server: https://kubernetes.default.svc
  source:
    path: argocd_applications/monitoring/thanos
    repoURL: git@gitlab.com:dsynapalos/cluster-setup.git
    targetRevision: main
  sources: []
  project: homelab
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
      enabled: true
    syncOptions:
      - CreateNamespace=true
