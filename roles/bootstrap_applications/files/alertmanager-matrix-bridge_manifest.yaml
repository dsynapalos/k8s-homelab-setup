apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alertmanager-matrix-bridge
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  destination:
    namespace: monitoring
    server: https://kubernetes.default.svc
  source:
    path: argocd_applications/monitoring/alertmanager-matrix-bridge
    repoURL: git@gitlab.com:dsynapalos/cluster-setup.git
    targetRevision: main
  sources: []
  project: homelab
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
      enabled: true
    syncOptions:
      - CreateNamespace=true
