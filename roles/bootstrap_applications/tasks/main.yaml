- name: upload argocd manifests
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', item) }}"
    kubeconfig: /etc/kubernetes/new_cluster_admin.conf
  loop: "{{ lookup('fileglob', role_path + '/files/*_manifest.yaml', wantlist=True) }}"