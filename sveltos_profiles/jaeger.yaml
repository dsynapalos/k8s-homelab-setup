apiVersion: config.projectsveltos.io/v1beta1
kind: ClusterProfile
metadata:
  name: jaeger
spec:
  clusterSelector:
    matchLabels:
      cluster: homelab
  syncMode: Continuous
  dependsOn:
    - harbor
  policyRefs:
    - kind: ConfigMap
      name: sveltos-app-jaeger
      namespace: projectsveltos
      deploymentType: Remote
